# Preview 속성 관련

## 내가 만든 레이아웃에 맞추기

```jsx
.previewLayout(.sizeThatFits)
```

## 디바이스에 맞추기

```jsx
.previewLayout(.device)
```

## 원하는 사이즈로 지정하기

```jsx
.previewLayout(.fixed(width: 100, height: 100))
```