# 이미지 애니메이션 관련

## 예시 코드
- 이미지를 받아올때, 성공과 실패, 비어있음을 구분하여 나타내 줄 수 있다.
    
    ~~~
    AsyncImage(url: URL(string: imageURL)) { phase in
        // 성공,실패, 비어있음
        if let image = phase.image {
            image.imageModifier()
        } else if phase.error != nil {
            Image(systemName: "ant.circle.fill").imageModifier()
        } else {
            Image(systemName: "photo.circle.fill").placeHolderModifier()
        }
    }.padding(40)
    ~~~
    
- 이미지를 받아올때, 이미지에 애니메이션을 추가할 수 있다.(아래서 위로 올라온다)
    
    ~~~
    AsyncImage(url: URL(string: imageURL), transaction: Transaction(animation: .spring(response: 0.5, dampingFraction: 0.6, blendDuration: 0.25))) { phase in
        switch phase {
        case .success(let image):
            image.imageModifier()
                .transition(.move(edge: .bottom))
        case .failure(_):
            Image(systemName: "ant.circle.fill").imageModifier()
        case .empty:
            Image(systemName: "photo.circle.fill").placeHolderModifier()
        @unknown default:
            ProgressView()
        }
    }.padding(40)
    ~~~
    
    옆에서 나온다.
    
    ~~~
    AsyncImage(url: URL(string: imageURL), transaction: Transaction(animation: .spring(response: 0.5, dampingFraction: 0.6, blendDuration: 0.25))) { phase in
        switch phase {
        case .success(let image):
            image.imageModifier()
                .transition(.slide)
        case .failure(_):
            Image(systemName: "ant.circle.fill").imageModifier()
        case .empty:
            Image(systemName: "photo.circle.fill").placeHolderModifier()
        @unknown default:
            ProgressView()
        }
    }.padding(40)
    ~~~
    
    가운데서 커진다.
    
    ~~~
    AsyncImage(url: URL(string: imageURL), transaction: Transaction(animation: .spring(response: 0.5, dampingFraction: 0.6, blendDuration: 0.25))) { phase in
        switch phase {
        case .success(let image):
            image.imageModifier()
                .transition(.scale)
        case .failure(_):
            Image(systemName: "ant.circle.fill").imageModifier()
        case .empty:
            Image(systemName: "photo.circle.fill").placeHolderModifier()
        @unknown default:
            ProgressView()
        }
    }.padding(40)
    ~~~